name: vision_chat_app
description: "AI Vision Chat Assistant — talk to AI with camera + microphone"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  camera: ^0.12.0+1
  record: ^7.1.0
  web_socket_channel: ^3.0.3
  audioplayers: ^6.7.1
  permission_handler: ^12.0.3
  image: ^4.5.0
  path_provider: ^2.1.0
  uuid: ^4.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
